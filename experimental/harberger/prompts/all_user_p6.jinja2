Instructions for this phase:

1. **Game Information**:
   - Phase: {{ phase_name }} (Phase {{ phase }})
   - Your Role: {{ role }} (Player #{{ player_number }})
   - Name: {{ player_name }}
   - Initial Shares: {{ initial_shares }}

2. **Market Information**:
   - Public Value Signal: {{ public_signal }}
   - Your Private Signal: {{ private_signal }}
   {% if current_orders %}
   - Current orders:
     {% for order in current_orders %}
     - {{ order.type }} | Price: {{ order.price }} | Quantity: {{ order.quantity }} | ID: {{ order.id }}
     {% endfor %}
   {% endif %}
   {% if your_orders %}
   - Your orders:
     {% for order in your_orders %}
     - {{ order.type }} | Price: {{ order.price }} | Quantity: {{ order.quantity }} | ID: {{ order.id }}
     {% endfor %}
   {% endif %}
   {% if property_value %}
   - Your Property's Value: {{ property_value }}
   {% endif %}

3. **Your Decision Options**:
   Provide the output (one of these options) as a JSON object:
   A. Post a new order:
   {
       "action": "post-order",
       "order": {
           "price": <number>,
           "quantity": 1,
           "type": <"ask" or "bid">,
           "now": <true or false>
       }
   }

   B. Cancel an existing order:
   {
       "action": "cancel-order",
       "order": {
           "id": <order_id>,
       }
   }

   C. Do nothing:
   {
       "action": "do-nothing"
   }

Remember:
- You can only trade one share at a time
- Setting "now": true will execute at the best available price
- Orders can be canceled before they are fulfilled
